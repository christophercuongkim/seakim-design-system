Names an icon-only control. Mandatory on every `IconButton`.

```jsx
<Tooltip label="Add leg">
  <IconButton icon="plus" label="Add leg" />
</Tooltip>
```

1–4 words, no full stop. Never put a link or essential information inside one —
tooltips do not exist on touch.
