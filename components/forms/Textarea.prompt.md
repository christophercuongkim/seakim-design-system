Multi-line text entry — trip notes, trade messages, feedback.

```jsx
<Field label="Notes for the driver" hint="Optional">
  <Textarea rows={3} placeholder="Gate code, floor, anything useful" />
</Field>
```

Resizes vertically only, so it can never break a column. Default 4 rows.
